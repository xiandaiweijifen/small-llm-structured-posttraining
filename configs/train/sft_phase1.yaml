experiment_name: sft_phase1
task_name: ticket_structured_output

model:
  base_model: TODO
  max_seq_length: 2048

training:
  method: sft
  learning_rate: 2e-5
  num_train_epochs: 3
  per_device_train_batch_size: 2
  gradient_accumulation_steps: 8
  warmup_ratio: 0.03

output:
  save_dir: results/checkpoints/sft_phase1
